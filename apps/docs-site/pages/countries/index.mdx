# Country guides

34 country adapter(s) shipped under `crates/report-*`.

Each adapter ships a typed Provider + MockProvider substrate today; live national tax-authority transport lands in follow-up `*-http` crates.
