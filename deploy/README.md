# deploy/

Deployment artifacts — not a Cargo workspace member.

- `docker-compose.yml` — single-host managed-stack deployment (T-1300).
- `helm/` — Kubernetes Helm chart (T-1301).
- `terraform/` — Terraform module for managed cloud (T-1302).

Scaffolded by bead **invoices-t-001-cargo-workspace-xos**.
